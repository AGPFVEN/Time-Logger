# To do later (not prioritary)
- Create a testing workflow

# To do
- Finish thinking how will be the app

# Doing
- Projects view
- Lateral sidebar

# Meanwhile:
    #DB Testing
        To test the db use:

        docker exec -i pg psql -U app -d appdb < database/tests/01-rows.sql

        Then explore the db to see if changes applied (I use the postgres vscode extension)
            localhost
            app
            secret
            5433